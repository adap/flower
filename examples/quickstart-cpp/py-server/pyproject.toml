[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "py-server"
version = "1.0.0"
description = ""
license = "Apache-2.0"
dependencies = ["flwr>=1.11.1", "numpy>=1.21.0"]

[tool.hatch.build.targets.wheel]
packages = ["."]

[tool.flwr.app]
publisher = "flwrlabs"

[tool.flwr.app.components]
serverapp = "py_server.server_app:app"
clientapp = ""

[tool.flwr.app.config]
num-server-rounds = 3
