# Schema
schema_version: 1.0.0
schema_type: mlcube_task

# Task Inputs
inputs:
        - name: input_file
          type: file

# Task Outputs
outputs:
        - name: log_dir
          type: directory
        - name: output_file
          type: file
