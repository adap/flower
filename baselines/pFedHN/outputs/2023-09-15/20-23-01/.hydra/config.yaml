dataset:
  data: cifar10
  root: ./data
model:
  device: cpu
  n_kernels: 16
  inner_lr: 0.005
  wd: 5.0e-05
  lr: 0.01
  in_channels: 3
  out_dim: 10
strategy:
  _target_: pFedHN.strategy.pFedHN
client:
  num_epochs: 50
  num_rounds: 5000
  num_nodes: 50
  batch_size: 64
  num_classes_per_node: 2
