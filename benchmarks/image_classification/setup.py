from setuptools import setup


setup(
   name='evaluation',
   version='1.0',
   description='Evaluation functions',
   packages=['evaluation'],
)

setup(
   name='static',
   version='1.0',
   description='Static configs',
   packages=['static'],
)
