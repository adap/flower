services:
  superlink:
    image: flwr/superlink:1.10.0.dev20240721
    command:
      - --insecure
    ports:
      - 9092:9092

  superexec:
    build:
      context: ${PROJECT_DIR:-.}
      dockerfile_inline: |
        FROM flwr/supernode:1.10.0.dev20240721

        WORKDIR /app
        COPY --chown=app:app pyproject.toml .
        RUN sed -i 's/.*flwr\[simulation\].*//' pyproject.toml \
          && python -m pip install -U --no-cache-dir .

        ENTRYPOINT ["flower-superexec"]
    command:
      - --executor
      - flwr.superexec.deployment:executor
      - --insecure
      - --executor-config
      - superlink=superlink:9091
    ports:
      - 9093:9093
    depends_on:
      - superlink
